设计文档（该文档杂七杂八的记录了一些设计和规范的问题）

## 服务端响应

```JSON
{
  "data": {},       // 服务器返回客户端需要的数据
  "success": true,  // 服务器返回业务信息是否处理成功
  "msg": "",        // 服务端返回业务消息
  "status": 1       // 服务端返回业务状态码
}
```

## 提交方式

```
GET：    获取资源，参数：params
POST：   增加资源，参数：data
PATCH：  修改资源，参数：data
DELETE： 删除资源，参数：params
```

```
列出所有用户：   GET     /user       data返回数组
列出某一用户：   GET     /user/:ID   data返回单个对象
新建一个用户：   POST    /user       data返回单个对象
修改一个用户：   PATCH   /user/:ID   data返回单个对象
删除一个用户：   DELETE  /user/:ID   data返回空文档
进行业务操作：   
```

## 集合参数

```
过滤条件： filter
排序：     order、sort
字段选择： fields
分页：     offset、limit
```

## 状态码

```
200 – OK – 一切正常
201 – OK – 新资源已经被创建
204 – OK – 资源删除成功

304 – 没有变化，客户端可以使用缓存数据

400 – Bad Request – 调用不合法，确切的错误应该在error payload中描述，例如：“JSON 不合法 ”
401 – 未认证，调用需要用户通过认证
403 – 不允许的，服务端正常解析和请求，但是调用被回绝或者不被允许
404 – 未找到，指定的资源不存在
422 – 不可指定的请求体 – 只有服务器不能处理实体时使用，比如图像不能被格式化，或者重要字段丢失。

500 – Internal Server Error – 标准服务端错误，API开发人员应该尽量避开这种错误
```
