import $ from '@/lib/ajax';

export default {
  // apiName: $.get('/api/url')
  // apiName: (arg1, arg2) => $.get('/api/url'),
  // zhangqin: () => $.get('api/xxx')
};

/**
 * @example config参数参看 https://www.npmjs.com/package/axios#request-config
   $.request(config);
   $.get(url, config);
   $.delete(url, config);
   $.head(url, config);
   $.post(url, data, config);
   $.put(url, data, config);
   $.patch(url, data, config)
 */
